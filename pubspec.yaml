name: flutter_cool_weather
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  built_value: ^8.1.0
  redux: ^5.0.0
  http: ^0.13.3
  built_collection: ^5.1.0
  flutter_redux: ^0.8.2
  flutter_spinkit: ^5.0.0
  redux_epics: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  built_value_generator: ^8.1.0
  build_runner: ^2.0.5

dependency_overrides:
  rxdart: ^0.27.1

flutter:
  uses-material-design: true
